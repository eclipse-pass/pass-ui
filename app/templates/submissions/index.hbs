{{#if (get (await currentUser.user) 'isSubmitter')}}
  {{#link-to "submissions.new" class="btn btn-primary btn-small pull-right mt-3"}}Create new Submission{{/link-to}}
{{/if}}

<h1 class="font-weight-light">Submissions</h1>

{{models-table
  data=submissionsToShow
  columns=columns
  themeInstance=themeInstance
  showColumnsDropdown=false
  useFilteringByColumns=false
  filteringIgnoreCase=true
  multipleColumnsSorting=false
  authorSelected='authorclick'
}}

{{#message-dialog show=messageShow to=messageTo subject=messageSubject message=messageText}}
{{/message-dialog}}
