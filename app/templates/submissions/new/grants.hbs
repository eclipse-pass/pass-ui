<WorkflowWrapper @title={{publication.title}} @isProxySubmission={{submission.isProxySubmission}} @submissionEvents={{submissionEvents}} @loadTab={{action "loadTab"}}>
<WorkflowGrants @submission={{submission}} @preLoadedGrant={{preLoadedGrant}} @next={{action "loadNext"}} @back={{action "loadPrevious"}} @abort={{action "abort"}} />
</WorkflowWrapper>
