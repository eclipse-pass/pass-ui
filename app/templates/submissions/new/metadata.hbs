{{#workflow-wrapper
  model=model
  loadTab=(action "loadTab")
}}
{{workflow-metadata
  didNotAgree=didNotAgree
  model=model
  next=(action "loadNext")
  back=(action "loadPrevious")
  hasProxy=hasProxy
}}
{{/workflow-wrapper}}
