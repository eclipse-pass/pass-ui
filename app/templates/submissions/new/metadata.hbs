{{#workflow-wrapper
  submission=submission
  title=publication.title
  isProxySubmission=submission.isProxySubmission
  submissionEvents=submissionEvents
  loadTab=(action 'loadTab')
  updateCovidSubmission=(action "updateCovidSubmission")
}}
{{workflow-metadata
  submission=submission
  publication=publication
  repositories=repositories
  next=(action "loadNext")
  back=(action "loadPrevious")
  abort=(action "abort")
}}
{{/workflow-wrapper}}
