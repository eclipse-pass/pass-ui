<WorkflowWrapper @title={{publication.title}} @isProxySubmission={{submission.isProxySubmission}} @submissionEvents={{submissionEvents}} @loadTab={{action "loadTab"}}>
<WorkflowMetadata @submission={{submission}} @publication={{publication}} @repositories={{repositories}} @next={{action "loadNext"}} @back={{action "loadPrevious"}} @abort={{action "abort"}} />
</WorkflowWrapper>
