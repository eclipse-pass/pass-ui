<WorkflowWrapper @title={{publication.title}} @isProxySubmission={{submission.isProxySubmission}} @submissionEvents={{submissionEvents}} @loadTab={{action "validateAndLoadTab"}}>
<WorkflowFiles @submission={{submission}} @previouslyUploadedFiles={{files}} @newFiles={{newFiles}} @next={{action "loadNext"}} @back={{action "loadPrevious"}} @abort={{action "abort"}} />
</WorkflowWrapper>
