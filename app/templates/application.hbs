
<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  {{#link-to 'index' class='navbar-brand'}}
    <img src='{{rootURL}}logo.png' alt='Logo' >
  {{/link-to}}

   <ul class="navbar-nav mr-auto">
    <li class="nav-item">
      {{#link-to 'index' class='nav-link'}}Home{{/link-to}}
    </li>
    <li class="nav-item">
      {{#link-to 'about' class='nav-link'}}About{{/link-to}}
    </li>
    <li class="nav-item">
      {{#link-to 'contact' class='nav-link'}}Contact{{/link-to}}
    </li>
  </ul>

  {{#if session.authenticated}}
    <span class="navbar-text ml-5">
      Logged in as:
      <span class='text-info'>
        {{session.user.username}} ({{session.user.role}})
      </span>
    </span>

     <button {{action 'logout'}} class="btn btn-warning btm-sm ml-2">Logout</button>
   {{/if}}
</nav>

<div class='container text-center'>
  <h1>PASS</h1>
  <p>Public Access Submission System</p>
</div>

<div class='container-fluid'>
  {{outlet}}
</div>

{{! Footer}}
<div class='container mt-4'>
  <p class='text-center'>
    <small>Making Public Access Policy compliance easier.</small>
  </p>
  <p class='text-center'>
    <small>rev. {{app-version hideVersion=true}}</small>
  </p>
</div>
