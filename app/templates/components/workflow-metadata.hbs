{{!-- takes in a schema and the model to save the schema to--}}
<h4 class="font-weight-light pull-right" style="margin-top:6.5px;">
  Form {{displayFormStep}} of {{schemas.length}}
</h4>
{{metadata-form didNotAgree=didNotAgree schema=schema model=model.newSubmission doiInfo=doiInfo currentFormStep=currentFormStep didAgree=didAgree nextForm=(action "nextForm") previousForm=(action "previousForm")}}
{{yield}}
