{{#power-select
  search=(action "searchGrants")
  placeholder="Select from your grants"
  onchange=(action "onSelect")
  as |grant|
}}
  {{grant.awardNumber}}: {{grant.projectName}}
{{/power-select}}
